import React, { Component } from "react";
import styled from "styled-components";
import { connect } from "react-redux";

const TopicInfo = () => {
  return <></>;
};

const mapStateToProps = state => {
    ;
}

export default connect()(TopicInfo);
